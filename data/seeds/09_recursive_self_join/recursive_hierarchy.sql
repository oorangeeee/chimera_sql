WITH RECURSIVE hierarchy AS (SELECT id, username, manager_id, 0 AS lvl FROM t_users WHERE manager_id IS NULL UNION ALL SELECT e.id, e.username, e.manager_id, h.lvl + 1 FROM t_users e INNER JOIN hierarchy h ON e.manager_id = h.id) SELECT id, username, manager_id, lvl FROM hierarchy ORDER BY id
